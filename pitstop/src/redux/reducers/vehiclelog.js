// TODO: Remove this later
import logEntries from './../../fakedata/logentries'

const vehiclelogReducer = function logentries(state = logEntries, action) {
    switch (action.type) {

        default: return state

    }
}

export default vehiclelogReducer
